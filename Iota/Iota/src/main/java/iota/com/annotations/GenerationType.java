package iota.com.annotations;

// Enum for different generation strategies
public enum GenerationType {
    AUTO, // Automatically determined by the database
    SEQUENCE, // Sequence generation
    IDENTITY // Identity column
}
